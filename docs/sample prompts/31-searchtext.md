# Exception-only conversion handoff ## Purpose The shopface engine remains canonical for discovery, scoring, verified fact snapshots, preview generation, human review, private delivery, expiry and teardown. This Convex slice is a server-only conversion ledger after intent. It does not replace the engine, render previews, send routine mail, create checkouts, or take payments. ## Automated lifecycle 1. The engine sends a signed `preview.sent` event only after all eight review/safety booleans are true and immutable preview/snapshot/expiry data is present. 2. The server persists one handoff plus a