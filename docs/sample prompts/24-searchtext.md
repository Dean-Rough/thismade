# shopface production fonts and logo correction ## Approved source The owner-provided Shopface Design System archive (Dropbox share supplied on August 25, 2026) is the asset source used for this correction. Its `tokens/fonts.css` replaces the previous Space Grotesk direction with `Archivo` for display and interface/body typography, retaining `IBM Plex Mono` for system labels, preview metadata and terminal-style details. ## Applied fonts - `Archivo` is loaded with `next/font/google` as the locally self-hosted production font. It supplies `--font-sans` and `--font-heading`, so headings, body cop